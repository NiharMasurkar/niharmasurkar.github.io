source "https://rubygems.org"

# Use the github-pages gem so your local build matches GitHub Pages exactly.
gem "github-pages", group: :jekyll_plugins

# If you'd rather run vanilla Jekyll instead of the GitHub Pages bundle,
# comment the line above and uncomment this one:
# gem "jekyll", "~> 4.3"

# Windows / JRuby helpers (harmless elsewhere)
gem "tzinfo-data", platforms: [:mingw, :mswin, :x64_mingw, :jruby]
gem "wdm", "~> 0.1.1", platforms: [:mingw, :mswin, :x64_mingw]
